# 데이터 종류
    # 수치형 (시퀀셜)
        # 연속형 => 실수
        # 이산 => 정수
        # 수치형 데이터 확인 방법
            # 1) describe
            # 2) boxplot => min (0) / 1사분위 (25) / 2사분위 (50) / 3사분위 (75) / max (100) 확인
            # 3) histogram => 분포 확인
    # 범주형 (카테고리)
        # 명목형 => T / F
        # 순서형 => 1 (만족) / 2 (보통) / 3 (불만족)
        # 범주형 데이터 확인 방법
            # 1) group by + counts => 컬럼별 그룹핑
            # 2) value_counts => 분포 확인
            # 3) barplot / pie
